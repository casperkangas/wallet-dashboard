# Development Notes

## Build System
- **Maven Wrapper**: Always use `./mvnw` instead of `mvn` to ensure consistency across different environments. Do not assume the local machine has the correct version of Maven installed globally.

## Version Control
- **Manual Commits**: Do not automatically commit changes at the end of a task. The user prefers to review all code modifications and commit them manually.
